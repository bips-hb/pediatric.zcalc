% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_calculator.R
\name{MetSScore}
\alias{MetSScore}
\title{Calculate Metabolic Syndrome (MetS) Score}
\usage{
MetSScore(df)
}
\arguments{
\item{df}{A data frame containing the following columns: `waist_z.score`, `homa_z.score`, `sbp_z.score`, `dbp_z.score`, `trg_z.score`, and `hdl_z.score`.
These are expected to be numeric vectors representing z-scores.}
}
\value{
A numeric vector representing the calculated MetS score for each row in the input data.
}
\description{
Computes a composite MetS score from standardized z-scores of metabolic indicators.
}
\details{
The MetS score is computed as:\cr
`waist_z + homa_z + 0.5 * (sbp_z + dbp_z + trg_z - hdl_z)`
}
\examples{
df <- data.frame(
  waist_z.score = c(1.2, 0.5),
  homa_z.score = c(0.8, 0.6),
  sbp_z.score = c(0.7, 0.3),
  dbp_z.score = c(0.6, 0.4),
  trg_z.score = c(1.0, 0.9),
  hdl_z.score = c(-0.5, -0.2)
)
MetSScore(df)

}
